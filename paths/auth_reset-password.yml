post:
  summary: Reset the user's password.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../requests/ResetPasswordRequest.yml"
  responses:
    200:
      description: Password reset successfully.