post:
  summary: Connect a new platform.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../requests/PlatformConnectRequest.yml"
  responses:
    200:
      description: Platform connected successfully.