post:
  summary: Request a password reset link.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../requests/ForgotPasswordRequest.yml"
  responses:
    200:
      description: Password reset request processed successfully.