get:
  summary: View analytics for a specific piece of content.
  parameters:
    - $ref: "../parameters/path/ContentIDParam.yml"
  responses:
    200:
      description: Analytics data retrieved successfully.
      content:
        application/json:
          schema:
            $ref: "../schemas/ContentAnalytics.yml"