type: object
properties:
  errors:
    type: array
    items:
      $ref: "./ErrorDetails.yml"
required:
  - errors
example:
  errors:
    - message: "Must be a valid email address."
      target: "email"
    - message: "Must not be empty."
      target: "password"
