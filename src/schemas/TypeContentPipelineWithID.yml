type: object
properties:
  pipelineID:
    description: ID to uniquely identify the saved pipeline.
    type: string
    format: uuid
  platform:
    $ref: ./TypeSupportedPlatform.yml
  steps:
    type: array
    items:
      $ref: ./TypePipelineStep.yml
required:
  - pipelineID
  - platform
  - steps
