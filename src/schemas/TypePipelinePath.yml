type: object
description: An object which contains path or sequence of pipeline steps to be
  followed before publishing the content to a specified platform.
properties:
  platform:
    $ref: ./TypeSupportedPlatform.yml
  steps:
    type: array
    items:
      $ref: ./TypePipelineStep.yml
required:
  - platform
  - steps
