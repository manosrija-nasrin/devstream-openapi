type: string
description: A name to identify the created pipeline for hoomans (intended).
  The pipeline will be saved under user account with this name.
  For temporary pipelines, a random name will be generated.
minLength: 1
