type: object
properties:
  userID:
    type: string
  totalContent:
    type: integer
    description: Total number of content the user has uploaded.
  totalViews:
    type: integer
    description: Total engagement count or view count for the content over different platforms.
      This is subjected to change in favour of an array of with eacch social media and their
      respective measurement of engagement or interaction.
required:
  - userID
  - totalContent
  - totalViews
